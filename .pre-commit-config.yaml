# Execute "pre-commit autoupdate" to update pre-commit config to the latest tagged version.

default_language_version:
    python: python3

repos:
  - repo: https://github.com/psf/black
    rev: 23.3.0
    hooks:
      - id: black
        name: Format code